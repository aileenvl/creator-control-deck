# Serial port override (otherwise auto-detect /dev/cu.usbmodem* then /dev/cu.usbserial*)
# PORT=/dev/cu.usbmodem14101

# Optional: TypeSafe / Jev Choice for CAPTURE routing (omit → inbox.md + mock Choice)
# TYPESAFE_API_KEY=tsk_...

# Simulation only: shorten the meeting timer (positive integer milliseconds)
# SIMULATE_MEETING_MS=3000
